Global setup:
  Download and install Git
  git config --global user.name "user name"
        
Next steps:
  mkdir lang
  cd lang
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:<user name>/lang.git
  git push origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:<user name>/lang.git
  git push origin master
      
Importing a SVN Repo?
  Click here
      
When you're done:
  Continue

